<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginHorizontal="16dp"
    tools:context=".ProfileActivity">


    <ImageView
        android:id="@+id/profileActivity_imageView"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="48dp"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        app:srcCompat="@drawable/profile_image" />

    <TextView
        android:id="@+id/user_name_set"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/profileActivity_imageView"
        android:layout_marginTop="24dp"
        android:textAlignment="center"
        android:text="User name"
        android:textSize="36sp"
        />


    <TextView
        android:id="@+id/user_bio_set"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/user_name_set"
        android:layout_marginTop="24dp"
        android:hint="Bio"
        android:textSize="20sp"
        android:padding="12dp"
        android:background="@drawable/field_design"/>

    <Button
        android:id="@+id/update_settings_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/user_bio_set"
        android:padding="8dp"
        android:layout_marginTop="24dp"
        android:text="Send message"
        android:textSize="18sp"
        android:background="@drawable/button_design"
        android:textColor="@color/white"
        app:backgroundTint="@color/black"/>





</RelativeLayout>